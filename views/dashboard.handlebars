<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Dashboard | Cricket Score Tracker</title>
  <style>
    body {
      background-image: url('https://cdn.wallpapersafari.com/42/32/NcZXHD.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: Arial, sans-serif;
    }
    .container {
      background: rgba(0, 0, 0, 0.8);
      padding: 40px;
      border-radius: 15px;
      text-align: center;
      color: white;
      width: 400px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.8);
    }
    h1 {
      font-size: 40px;
      margin-bottom: 25px;
    }
    .button-group {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    a.button {
      background: #ffc107;
      color: black;
      padding: 14px;
      border-radius: 10px;
      text-decoration: none;
      font-size: 18px;
      font-weight: bold;
      transition: background 0.3s;
    }
    a.button:hover {
      background: #e0a800;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Dashboard</h1>
    <div class="button-group">
      <a href="/teams" class="button">Manage Teams</a>
      <a href="/players" class="button">Manage Players</a>
      <a href="/matches" class="button">Manage Matches</a>
      <a href="/scores" class="button">Manage Scores</a>
      <a href="/tournaments" class="button">Manage Tournaments</a>
      <a href="/logout" class="button" style="background: #dc3545; color: white;">Logout</a>
    </div>
  </div>
</body>
</html>
